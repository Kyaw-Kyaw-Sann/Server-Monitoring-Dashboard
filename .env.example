# This demonstration reads public/data.json and needs no environment variables.
# For a future hosted API, expose only a safe public URL:
# VITE_MONITORING_API_URL=https://example.com/system-health.json
